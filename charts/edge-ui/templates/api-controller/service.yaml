apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-api-server.fullname" . }}
  namespace: {{ .Values.apiController.namespace }}
  labels:
    {{- include "edge-api-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.apiController.service.type }}
  ports:
    - port: {{ .Values.apiController.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "edge-api-server.selectorLabels" . | nindent 4 }}
